import theme, { colors } from './spectacleTheme';
import MeteorLogo from './MeteorLogo';
import { coverContent, coverProps } from './coverContent';
import { defaultSlideProps } from './defaultSlideProps';

export {
  theme, colors,
  MeteorLogo,
  coverContent, coverProps,
  defaultSlideProps,
};
